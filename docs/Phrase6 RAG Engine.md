一、阶段目标：构建完整RAG ENGINE

二、当前环境

三、阶段一

1. 目标：部署验证本地LLM能力
2. 过程概览：
3. 详细步骤：
   1. 系统程序，java端服务，parser和embedding服务共占8132MiB显存, 约8GB，总显存24GB
   1. 下载Qwen3-8B-AWQ模型，使用vllm启动
   1. 模型运行正常，接口可以调用
   1. Qwen3-8B-AWQ模型占用显存11208MiB显存, 约10.9GB
4. 结果：

四、阶段二

1. 目标：建立RAG的数据类和模型调用服务
2. 过程概览：
3. 详细步骤：
   1. 在rag/model目录下新建RAG问答请求类RagRequest、文档片段类RagCitation、RAG业务状态枚举RagStatus，RAG问答结果类RagResponse。
   2. 在rag/llm目录下新建LLM接口类LlmClient，异常类LlmClientException，LLM调用类HttpLlmClient。
   3. 在rag/context目录下新建ContextBuilder用于整合检索结果
   4. 在rag/prompt目录下新建RagPromptBuilder用于将问题和整合后的检索结果组合为prompt
   5. 在application.yml新增llm和rag配置
4. 结果：

五、阶段三

1. 目标：构建RAG问答服务
2. 过程概览：
3. 详细步骤：
   1. 在 rag/service 下新建 RagService 用于RAG问答编排，新建 CitationValidator 用于校验模型回答的引用编号
   2. 在 rag/controller 下新建 RagController，提供RAG问答接口
   3. RAG问答接口验证通过，可正常回答问题
4. 结果：

六、阶段四

1. 目标：正式验证RAG准确度

2. 过程概览：

3. 详细步骤：

   1. 基于300篇入库文档，使用大模型构建120个评测问题, 覆盖单Chunk、同文档多Chunk、跨文档、证据不足、限定documengIds等。

      ```
      {
      	"id":"SC001",
      	"question":"《节能装备高质量发展实施方案（2026—2028年）》提出，到2028年节能装备发展要达到哪些总体成效？",
      	"category":"single_chunk",
      	"expectedStatus":"ANSWERED",
      	"referenceChunkIds":["6ab43b785378c05064f86134"],
      	"answerKeywords":["到 2028 年","国际领先","人工智能赋能节能装备应用场景"],
      	"answerKeyPoints":["节能装备关键材料、零部件取得突破，重点行业领域用能系统匹配性和实际运行效率持续提升。","电机、变压器等节能装备能效水平达到国际领先。","推广系统解决方案、打造人工智能赋能应用场景，并培育骨干企业和产业集群。"]
      }
      ```

   2. 设置通过要求

      | 检查项               | 当前通过条件                                                 |
      | -------------------- | ------------------------------------------------------------ |
      | `status`             | 返回状态等于题目标注的 `ANSWERED` 或 `INSUFFICIENT_EVIDENCE`。 |
      | `referenceRetrieved` | 查询到所有相关的Chunk                                        |
      | `referenceCited`     | 回答时引用所有相关Chunk                                      |
      | `answerKeywords`     | 答案包含**每一个**标注关键词                                 |
      | 引用与范围           | 返回的引用编号须出现在答案里；指定了 `documentIds` 时，检索结果和引用都不得越界；拒答题不得返回引用。 |

   3. 挑选30道问题，用于初步评测。

   4. 初始tempeature=0.7，结果不稳定，无法测试，故采用tempature=0

   5. rerank=false

      > 正确：22 题，73.3%
      >
      > 部分正确：7 题，23.3%
      >
      > 错误：1 题，3.3%
      >
      > 至少包含有效答案：29 题，96.7%
      >
      > **部分正确的7题：**
      >
      > | ID       | 主要问题                                                     |
      > | -------- | ------------------------------------------------------------ |
      > | `SC004`  | 正确答出缺水、水资源超载等重点地区，但混入安全应急装备和机械行业内容，且遗漏“全链条保障”和“高端化、智能化、绿色化”。 |
      > | `SMC001` | 电机、风机、泵、压缩机和变压器占比全部正确，但遗漏总体目标“能效水平达到国际领先”。 |
      > | `SMC005` | 答出98%和每月100万平方公里两项指标，但遗漏天空地海一体化愿景及运维智能化70%，并混入固废指标。 |
      > | `SMC013` | 正确回答“一网统管”和风险监测预警措施，但遗漏50个以上城市、“一件事”覆盖目标及全灾种数字化预案库。 |
      > | `SMC020` | 回答了部分创新协同做法，但没有给出不少于100个试点、60个应用场景，也遗漏“发榜—揭榜”，且混入其他文档内容。 |
      > | `CD007`  | 正确说明2028年为2030年奠定基础，并答出2.5万亿元，但遗漏50000家企业、20个行业、5万张5G专网和207个工业中类。 |
      > | `CD010`  | 零碳工厂目标及单位GDP排放降低17%均正确，但遗漏“产品碳足迹管理体系基本建成”。 |
      >
      > **错误：**
      >
      > - `CD004`：两个原始 Chunk 已分别明确给出“不少于25个品牌”“300项以上标准、10项以上国际标准、转化率85%以上”，但系统回答“无法根据提供的资料确认”，属于错误拒答。

   6. rerank=true

      > 正确：23 题（76.7%）
      > 部分正确：6 题（20.0%）
      > 错误：1 题（3.3%）
      > 至少部分正确：29/30（96.7%）
      >
      > 
      >
      > **部分正确的 6 题：**
      >
      > - `SMC001`：设备比例正确，缺少“总体能效达到国际领先水平”。
      > - `SMC005`：回答了多项指标，但缺少天空地海一体化总体目标，且未准确覆盖作业智能化指标。
      > - `SMC013`：回答了“一网统管”和风险预警，但缺少50个以上城市、“一件事”和全灾种数字预案库。
      > - `SMC020`：回答了技术布局和揭榜机制，但缺少100个试点、60个典型应用等数量目标。
      > - `CD004`：25个品牌、300项以上标准、10项以上国际标准均正确，但漏了国际标准转化率达到85%以上。
      > - `CD007`：递进关系和207个工业中类正确，但漏了5万家企业、20个行业、5万张5G专网、2.5万亿元等关键规模指标。
      >
      > **错误**
      >
      > - `CD010`。rerank=true 只召回了零碳工厂文档的 Chunk，没有召回国家气候规划参考 Chunk `6ab43b965378c05064f861e9`，因此模型回答“无法确认”。从当前上下文看拒答是合理的，但从端到端 RAG 评测看属于召回失败。

   7. 对比

      > | 指标              | rerank=false | rerank=true |
      > | ----------------- | ------------ | ----------- |
      > | 语义正确          | 22           | **23**      |
      > | 部分正确          | 7            | **6**       |
      > | 错误              | 1            | 1           |
      > | 自动通过          | 15           | **17**      |
      > | 平均耗时          | 1295 ms      | 1529 ms     |
      > | 参考 Chunk 被召回 | 24           | 24          |
      > | 参考 Chunk 被引用 | 19           | 18          |
      >
      > 主要变化：
      >
      > - `SC004`：部分正确 → 正确，rerank 清除了无关文档干扰。
      > - `CD004`：错误 → 部分正确，跨文档召回得到改善。
      > - `CD010`：部分正确 → 错误，rerank 反而丢失了其中一个跨文档证据。
      >
      > 总体来看，rerank=true 的语义正确数只提升了 **1题**，耗时增加约 **18%**；单 Chunk 效果更稳定，但跨文档召回仍不稳定。

4. 结果：

本周决策

遗留问题

下周计划